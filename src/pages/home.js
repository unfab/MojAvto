// Home page — MojAvto.si
// TODO: Phase 2 — migrate from localStorage to Firestore
// For now, the existing js/main.js handles home page logic via router.js

export function initHomePage() {
    // Home page is currently handled by the legacy js/main.js
    // This will be replaced in Phase 2 with Firestore-connected listing grid
    console.log('[HomePage] init');
}
